server:
  port: 8085
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin-sleuth
          uri: https://github.com/HarDep/microservicios-1.git
          search-paths: config-data
  application:
    name: config-service
